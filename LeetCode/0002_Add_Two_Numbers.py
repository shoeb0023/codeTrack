        carry != 0:
            total_sum = carry
            
        while l1 is not None or l2 is not None or 
        
        carry = 0
        curr = dummy
        dummy = ListNode(0)
    l2: Optional[ListNode]) -> Optional[ListNode]:
    def addTwoNumbers(self, l1: Optional[ListNode], 
class Solution:

#         self.next = next
#         self.val = val
#     def __init__(self, val=0, next=None):
# class ListNode:
# Definition for singly-linked list.
